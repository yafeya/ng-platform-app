import { HeroValidatorDirective } from './hero-validator.directive';

describe('HeroValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
